# Simple Marathi program

# Variables
अ = 5
ब = 10
योग = अ + ब
छापा("अ + ब = " + योग)

# If statement
जर अ < ब:
    छापा("अ हा ब पेक्षा लहान आहे")
नाहीतर:
    छापा("अ हा ब पेक्षा मोठा आहे")

# While loop
संख्या = 3
जोपर्यंत संख्या > 0:
    छापा(संख्या)
    संख्या = संख्या - 1

# Function
कार्य दुप्पट(क):
    परत क * 2
समाप्त

छापा("दुप्पट(5) = " + दुप्पट(5)) 